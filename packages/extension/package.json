{
  "name": "@pix/extension",
  "version": "0.2.0",
  "private": true,
  "description": "Browser extension that records the user's interaction with a web page as a PiX score. Chrome (MV3) and Firefox (WebExtensions).",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git://github.com/eadpucv/pix.git",
    "directory": "packages/extension"
  },
  "license": "MIT",
  "scripts": {
    "build": "node build.mjs chrome && node build.mjs firefox",
    "build:chrome": "node build.mjs chrome",
    "build:firefox": "node build.mjs firefox",
    "watch:chrome": "node --watch build.mjs chrome"
  },
  "dependencies": {
    "@pix/core": "*"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
