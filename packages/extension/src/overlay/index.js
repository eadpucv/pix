// PiX Recorder — overlay
// Injected as iframe (web_accessible_resource) into every tab while
// recording. Renders the red-light indicator. Stage 1: static markup
// in the HTML; this script is a no-op placeholder for later state
// updates (pause / step counter / stop button).
